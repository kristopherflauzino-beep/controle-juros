# Controle de Juros

Aplicação web completa em Next.js para controle de clientes, solicitações, acordos, Tabela Price, juros diário composto e geração de PDF.

## Requisitos

- Node.js 20 ou superior
- PostgreSQL compatível com Vercel
- Variáveis de ambiente:
  - `DATABASE_URL`
  - `JWT_SECRET`

## Rodar localmente

```bash
npm install
copy .env.example .env
npx prisma db push
npm run dev
```

Acesse:

```text
http://localhost:3000
```

## Primeiro acesso

Abra `/first-access` e cadastre o administrador. Depois faça login em `/login`.

## Testar build

```bash
npm run build
```

## Deploy na Vercel

1. Envie o projeto para o GitHub na branch `principal`.
2. No projeto da Vercel, configure:
   - `DATABASE_URL`
   - `JWT_SECRET`
3. Faça o deploy.
4. A rota `/api/health` deve retornar `OK`.

## GitHub solicitado

Repositório previsto:

```text
https://github.com/kristopherflauzino-beep/controle-juros
```

Branch prevista:

```text
principal
```

## Observação importante

O projeto foi gerado do zero. O arquivo `package.json` contém apenas JSON válido e nenhum CSS. O CSS fica somente em `src/app/globals.css`.
